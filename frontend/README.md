# UrbanNxt Frontend (Next.js + SCSS)

- Dev: `npm i` then `npm run dev` (defaults to http://localhost:5173)
- API base URL: http://127.0.0.1:8000/api
- Structure mirrors your request with `src/components`, `src/pages`, `src/context`, `src/services`, `src/styles`.
